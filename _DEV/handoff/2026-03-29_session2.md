# 2026-03-29 세션 2 — 네비 정리 + 관리자 + UI 개선

## 세션 요약
바텀 내비 구조 정리, 프로필을 헤더로 이동, 하위 페이지 UI 패턴 적용, 관리자 페이지 구축, 소개/공지 페이지 추가, 사진 압축/뷰어, 닉네임 수정 등 다수 개선.

## 완료 작업

### 1. 헤더 태그라인 수정
- "시민 산불 감시" → "시민 산불 감시 플랫폼"

### 2. 바텀 내비 재구성
- 기존 3탭(지도/관찰/나의 기여도) → **3탭(홈/관찰/소개)**
- 프로필 탭 제거 → 헤더 우측으로 이동
- 배경: `bg-gray-100/80` 연한 그레이 (보더 없음)

### 3. 프로필 → 헤더 우측 이동
- 로그인 시: LevelBadge `sm` (등급 아이콘 원형 뱃지)
- 비로그인 시: UserCircle 아이콘 → 탭하면 `/auth`로 이동
- TopBar에서 `useAuth` + `useProfile` 직접 사용

### 4. 메인 탭 헤더 통일
- 홈(`/`), 관찰(`/observe`), 소개(`/about`) 모두 동일한 브랜드 헤더
- 앱명 + 태그라인 + 프로필 아이콘, 뒤로가기 없음

### 5. 하위 페이지 UI 패턴 (`sub` 모드)
- TopBar `sub` prop: 흰색 배경 + 중앙 타이틀 + 좌측 뒤로가기
- PageLayout에 `sub`, `title` prop 추가
- 적용: Profile, AdminDashboard, AdminUsers, AdminNotices

### 6. 관리자 시스템

#### DB 마이그레이션 (`20260329_005_admin_role.sql`)
- `profiles.role` 컬럼 (기본값 `'user'`)
- `is_admin()` 함수 (SECURITY DEFINER — RLS 재귀 방지)
- 관리자용 RLS 정책 (profiles, observations 전체 조회)

#### 관리자 대시보드 (`/admin`)
- 요약 통계: 전체 사용자, 오늘 활성, 전체 관측, 오늘 관측, 최근 7일
- 고위험 관측: risk_score 8+ (최근 7일)
- 아코디언: 최근 관측 피드 (10건) / 지역별 관측 분포 (17개 시도 바 차트)
- 관리 메뉴: 사용자 관리, 공지 관리

#### 사용자 관리 (`/admin/users`)
- 유저 목록 (닉네임, 포인트, 스트릭, 가입일, 관리자 뱃지)

#### 공지 관리 (`/admin/notices`)
- 작성/수정/삭제 폼 (제목, 내용, 상단 고정)
- 수정: 연필 아이콘 탭 → 폼에 채워짐 + "수정 중" 표시

#### 라우트
- `/admin` → RequireAdmin → AdminDashboard
- `/admin/users` → RequireAdmin → AdminUsers
- `/admin/notices` → RequireAdmin → AdminNotices

#### 프로필 연동
- 관리자인 경우에만 "관리자 대시보드" 링크 (Shield 아이콘)

### 7. 공지사항 (`20260329_006_notices.sql`)
- `notices` 테이블 (title, content, pinned, created_at, updated_at)
- RLS: 전체 공개 조회 / 관리자만 관리
- 소개 페이지 상단에 공지 표시 (3건까지 기본, 더보기)
- 아코디언 +/- 아이콘

### 8. 소개 페이지 (`/about`)
- 바텀 내비 3번째 탭
- 공지사항 + 프로젝트 소개 3카드 + 관측 방법 + CTA

### 9. 닉네임 관리 개선
- 회원가입 폼에 닉네임 필드 추가 (가입 시 바로 저장)
- 프로필에서 닉네임 탭 → 수정 모달 (✎ 아이콘)
- NicknameModal: `initialValue`, `onClose` prop 추가 (수정 모드 지원)

### 10. 사진 압축
- PhotoAttach에서 업로드 전 자동 압축 (1280px, JPEG 0.7)
- 원본 3~5MB → 약 200~400KB

### 11. 사진 뷰어
- ObservationSheet에 PhotoViewer 추가 (풀스크린 `bg-black/90`, z-[9999])
- SummaryCard 하단: 사진 썸네일 그리드 (6열, 스퀘어, gap 3px)
- SingleCard: 사진도 동일한 6열 그리드 썸네일
- 탭하면 전체화면 뷰어 (양쪽 20px 패딩)

### 12. 내 위치 마커 개선
- GeoJSON을 커스텀 pane(`zIndex: 200`)으로 분리 (마커 가림 방지)
- 줌 레벨별 크기 조절 (줌7: 20px → 줌13: 36px)
- CSS 클래스 기반 스타일 + 펄스 애니메이션
- `interactive: false` (관측 마커 클릭 방해 방지)

### 13. 내 관찰에 지역명 표시
- useMyObservations에서 lat/lng 추가 조회
- bbox 기반 시도명 매칭 → "강원 · 3월 29일 ..." 형태
- 카카오 로컬 API 연동 후 시군구로 업그레이드 예정

### 14. 아코디언 아이콘 통일
- ChevronRight → Plus/Minus (+/-) 로 전체 통일
- 적용: Profile, AdminDashboard, About (공지사항)

### 15. 기타
- 관찰 페이지 "관찰 입력" 타이틀 삭제
- AdminDashboard `created_at` → `observed_at` 버그 수정
- RLS 무한재귀 수정: `is_admin()` SECURITY DEFINER 함수

## 현재 관리자 계정
- ahnsujung@korea.kr — admin
- reverve9@naver.com — admin
- fefedeva@naver.com — user

## 마이그레이션 (Supabase SQL Editor)

### 1. 관리자 역할 (적용 완료)
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS role text NOT NULL DEFAULT 'user';

CREATE OR REPLACE FUNCTION is_admin()
RETURNS boolean AS $$
  SELECT EXISTS (
    SELECT 1 FROM profiles
    WHERE id = auth.uid() AND role = 'admin'
  );
$$ LANGUAGE sql SECURITY DEFINER STABLE;

CREATE POLICY "admins can read all profiles"
  ON profiles FOR SELECT USING (is_admin());

CREATE POLICY "admins can read all observations"
  ON observations FOR SELECT USING (is_admin());
```

### 2. 공지사항 (미적용)
```sql
CREATE TABLE notices (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  title text NOT NULL,
  content text NOT NULL,
  pinned boolean NOT NULL DEFAULT false,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);

ALTER TABLE notices ENABLE ROW LEVEL SECURITY;

CREATE POLICY "notices are public" ON notices
  FOR SELECT USING (true);

CREATE POLICY "admins can manage notices" ON notices
  FOR ALL USING (is_admin());
```

## 새 파일
- `src/hooks/useAdmin.js`
- `src/components/auth/RequireAdmin.jsx`
- `src/pages/About.jsx`
- `src/pages/admin/AdminDashboard.jsx`
- `src/pages/admin/AdminUsers.jsx`
- `src/pages/admin/AdminNotices.jsx`
- `supabase/migrations/20260329_005_admin_role.sql`
- `supabase/migrations/20260329_006_notices.sql`

### 16. PWA 설치 유도 UI
- **InstallBanner** (`src/components/pwa/InstallBanner.jsx`): 앱 전역 하단 팝업
  - Android/Desktop: `beforeinstallprompt` → 닫기/설치하기 풀너비 2열 버튼
  - iOS: Safari 공유 → 홈 화면에 추가 안내
  - 닫기 시 localStorage 저장, 7일 후 재노출
  - 이미 standalone이면 미표시
- **InstallCard** (`src/components/pwa/InstallCard.jsx`): 프로필 페이지 고정 카드
  - 카드 전체가 버튼 → 탭하면 바로 설치 프롬프트
  - iOS는 안내 텍스트만 표시
  - 설치 완료 시 자동 숨김, 닫기 버튼 없음
- App.jsx에 InstallBanner 추가, Profile.jsx에 InstallCard 추가
- manifest.json 수정: `any maskable` → `any`/`maskable` 분리, screenshots 추가 (wide + mobile)
- vite.config.js: `devOptions.enabled: true` 추가 (dev에서 SW 테스트용)
- 배포 후 HTTPS 환경에서 테스트 필요 (localhost에서 beforeinstallprompt 불안정)

## 새 파일
- `src/components/pwa/InstallBanner.jsx`
- `src/components/pwa/InstallCard.jsx`

## 내일 작업
- [ ] PWA 설치 배너/카드 배포 후 테스트
- [ ] 카카오 로그인 + 로컬 API 연동 (가이드: `2026-03-29_kakao_login_setup.md`)
- [ ] R2 스토리지 전환 (가이드: `2026-03-29_r2_storage_setup.md`)
- [ ] 역지오코딩으로 시군구 주소 저장
- [ ] notices 마이그레이션 적용
- [ ] 커밋 진행
