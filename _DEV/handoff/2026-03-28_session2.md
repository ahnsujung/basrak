# Handoff - 2026-03-28 Session 2 — UI 스타일 리팩토링 이어서

## 완료된 작업

### Phase 1: 구조적 결함 수정 ✅

- **에러 핸들링**: useLocalContribution, useMapObservations, useProfile에 .catch + error 상태 추가
- **훅 분리**: MyObservations → useMyObservations, PointHistory → usePointHistory 신규 훅, ObservationLayer의 supabase 호출 → useHeatmapGrid 훅
- **ErrorBoundary**: components/ui/ErrorBoundary.jsx 생성, App.jsx에서 Routes 감싸기
- **비동기 cleanup**: 모든 훅의 useEffect에 mounted flag 패턴 적용
- **기존 린트 에러 수정**: LevelBadge → levelUtils.js 분리, useLocation 리팩토링, MapTest queueMicrotask 처리

### Phase 2: 테마 시스템 ✅

- **constants/theme.js**: brand/risk 색상 상수 (JS용)
- **index.css @theme**: 브랜드/리스크/서피스/맵 색상, 폰트, fluid typography (text-xs~5xl clamp), fluid button heights
- **@layer components**: typo-page-title, typo-section-title, typo-section-label, typo-body, typo-caption, typo-caption-tiny
- **App.css 삭제**: Vite 템플릿 잔재
- **색상 하드코딩 교체**: 19개 파일 — `#2d6a4f` → `bg-brand`, `green-700` → `brand`, risk hex → `risk` import

### UI 스타일 개선 ✅

- **Lucide React 도입**: UI 아이콘 11개 교체 (Map, User, Camera, MapPin, Ban, AlertTriangle, CircleCheck, Mail, Flame, Shield, LocateFixed)
- **이모지 유지**: 🍂(브랜드), 🌱🌿🏔️(등급), 풍속 이모지, 🥇🥈🥉(순위)
- **max-width**: 430px → 512px (max-w-lg)
- **Pretendard 폰트**: CDN dynamic subset, index.html `<link>`로 로드
- **소개로드 스타일 참고 적용**:
  - TopBar: 중앙 타이틀, 서브페이지 뒤로가기 (ChevronLeft)
  - 그림자 → border 기반 flat 스타일 (Card, MapLegend, LocalContributionCard, FAB, EmptyMapOverlay)
  - 입력 필드: bg-gray-50, focus시 bg-white 전환
  - 리스트: border-gray-100 구분선, 세로 패딩 넓힘
  - Button secondary: border 추가

### 구조 정리 ✅

- **PageLayout**: TopBar + BottomNav 내장, title 고정 "바스락 🍂", fullWidth prop (지도용)
- **모든 페이지 헤더 통일**: "바스락 🍂" 고정, 페이지 타이틀은 콘텐츠 영역 h1으로
- **CSS Cascade Layers 수정**: `* { padding: 0 }` → `@layer base`로 이동 (Tailwind utilities 우선순위 보장)

---

## 현재 구조

```
PageLayout (공통)
  ├─ TopBar (중앙: "바스락 🍂", 좌: 뒤로가기, 우: topRight)
  ├─ <main> (px-5 기본, fullWidth면 패딩 없음)
  └─ BottomNav (지도/관찰/나의 기여도)

Home:     fullWidth, topRight=LevelBadge
Observe:  h1="관찰 입력"
Profile:  h1="내 기여", topRight=로그아웃
Auth:     hideNav
```

---

## 이어서 할 작업: UI 스타일 마무리

### 1. 소개로드 참고 세부 스타일 보정

**현재 상태**: 큰 틀은 잡혔으나 세부 디테일 보정 필요

- [ ] 관찰 페이지: DrynessSelector/WindSelector 카드 스타일 보정 (라운딩, 선택 상태 색상)
- [ ] 프로필 페이지: 스트릭 카드, 등급 배지 시각적 정리
- [ ] 탭 컴포넌트: 배경/활성 상태 스타일
- [ ] ObservationSheet: 하단 시트 디자인 개선

### 2. 반응형 미세 조정

- [ ] clamp 적용된 폰트/버튼 실기기 테스트
- [ ] 320px ~ 512px 범위에서 레이아웃 깨짐 확인
- [ ] BottomNav safe-area-inset 처리 (노치 기기)

### 3. 아이콘 최종 결정

- [ ] Lucide vs 이모지 하이브리드 최종 확인
- [ ] 아이콘 크기 일관성 점검

### 4. 색상/간격 미세 조정

- [ ] 소개로드처럼 text-gray 계열 단계 정리 (현재 gray-400/500/600/700/800 혼용)
- [ ] 섹션 간 간격 통일 (gap-5 vs gap-6)
- [ ] border-gray-100 vs gray-200 일관성

---

## 주의사항

- **CSS Cascade Layers**: `@layer` 밖에 글로벌 스타일 추가하지 말 것. Tailwind utilities가 무시됨
- **Pretendard**: index.html `<link>`로 로드 (CSS @import 아님 — TW4 충돌)
- **ObservationLayer**: daily_maps 조회 시 `.maybeSingle()` 사용 (데이터 없을 때 406 방지)

---

## 파일 변경 요약

**신규 6개**: useMyObservations.js, usePointHistory.js, ErrorBoundary.jsx, theme.js, levelUtils.js, .env.local
**삭제 1개**: App.css
**수정 40개**: 전체 목록은 `git diff --stat` 참조
**빌드**: 성공, 린트 에러 0개
